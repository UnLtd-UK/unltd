---
const { name, slug, link } = Astro.props;
---

<a
  href={ (slug && !link) ? `/${slug}` : `${link}`}
  target={(!slug && link) && "_blank" }
  class="block text-sm font-medium p-2 bg-gray-100 hover:bg-gray-300"
  data-dropdown="dropdownA"
>
  {name}
  {(!slug && link) && "+" }
</a>
