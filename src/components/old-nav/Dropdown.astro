---
const { navItem } = Astro.props;
import Item from "./Item.astro";
---

<button
  id={`dropdownButton-${navItem.slug}`}
  class="dropdownButton p-3 text-sm font-medium bg-violet-200 text-violet-800 hover:bg-violet-300 hover:text-violet-900 focus:font-bold focus:bg-violet-300 focus:text-violet-900"
  onclick={`desktop("${navItem.slug}")`}
  aria-expanded="false"
>
  {navItem.name} &#5167;
</button>

<ul
  id={`dropdownContent-${navItem.slug}`}
  class="flex-col gap-4 p-3 bg-violet-300 border border-sky-500 dropdownContent absolute hidden"
>
  {
    navItem.children.map((navItemChild) => (
      <li>
        <Item item={navItemChild} />
      </li>
    ))
  }
</ul>
