---
/**
 * FormCard Component
 * A distinctive card for form/template resources
 */

import Icon from "@components/Icon.astro";
<<<<<<< HEAD
import { getTopicLabel, getTopicConfig } from "@data/topics";
=======
>>>>>>> c40fddc (Refactor resources and spaces structure)

interface Props {
    name: string;
    description: string;
    href: string;
<<<<<<< HEAD
    topic?: string;
}

const { name, description, href, topic } = Astro.props;

const topicLabel = getTopicLabel(topic);
const topicConf = getTopicConfig(topic);
=======
    category?: string;
}

const { name, description, href, category } = Astro.props;

const categoryLabels: Record<string, string> = {
    legal: "Legal & Policies",
    application: "Application Help",
    funding: "Funding & Budget",
    business: "Business Support",
    wellbeing: "Wellbeing",
    general: "General",
};

const categoryLabel = category ? categoryLabels[category] || category : null;
>>>>>>> c40fddc (Refactor resources and spaces structure)
---

<a
    href={href}
    target="_blank"
    rel="noopener noreferrer"
    class="group relative flex overflow-hidden rounded-2xl bg-linear-to-br from-amber-900/60 via-orange-800/40 to-amber-900/60 ring-1 ring-amber-600/40 hover:ring-amber-400/60 transition-all duration-300 hover:shadow-xl hover:shadow-amber-500/15 hover:-translate-y-1"
>
    {/* Form icon sidebar */}
    <div
        class="flex flex-col items-center justify-center w-20 bg-linear-to-b from-amber-600/30 to-orange-700/30 border-r border-amber-500/20"
    >
        <div
            class="flex h-12 w-12 items-center justify-center rounded-xl bg-amber-500/30 ring-1 ring-amber-400/40 group-hover:bg-amber-500/40 transition-all duration-300"
        >
            <Icon
                name="file-lines"
                style="solid"
                class="h-6 w-6 text-amber-200 group-hover:text-amber-100"
            />
        </div>
    </div>

    <div class="flex-1 p-5">
        {/* Header */}
        <div class="flex items-start justify-between gap-3 mb-3">
            <span
                class="inline-flex items-center gap-1.5 rounded-full bg-amber-600/40 px-3 py-1 text-xs font-semibold text-amber-200 ring-1 ring-amber-500/40"
            >
                <Icon name="clipboard-list" style="solid" class="h-3 w-3" />
                Form / Template
            </span>
            {
<<<<<<< HEAD
                topicLabel && (
                    <span
                        class={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-medium ring-1 ${topicConf ? `${topicConf.bgClass} ${topicConf.textClass} ${topicConf.ringClass}` : "bg-amber-800/50 text-amber-300 ring-amber-600/40"}`}
                    >
                        {topicConf && (
                            <Icon
                                name={topicConf.icon}
                                style="solid"
                                class="h-3 w-3"
                            />
                        )}
                        {topicLabel}
=======
                categoryLabel && (
                    <span class="inline-flex items-center rounded-full bg-amber-800/50 px-2.5 py-1 text-xs font-medium text-amber-300 ring-1 ring-amber-600/40">
                        {categoryLabel}
>>>>>>> c40fddc (Refactor resources and spaces structure)
                    </span>
                )
            }
        </div>

        {/* Content */}
        <h3
            class="text-lg font-semibold text-amber-100 group-hover:text-white transition-colors duration-200 line-clamp-2 mb-2"
        >
            {name}
        </h3>
        <p class="text-sm leading-relaxed text-amber-300/80 line-clamp-2 mb-4">
            {description}
        </p>

        {/* Footer */}
        <div
            class="flex items-center gap-2 text-sm font-medium text-amber-400 group-hover:text-amber-300 transition-colors"
        >
            <span>Open form</span>
            <Icon
                name="arrow-up-right-from-square"
                style="solid"
                class="h-3.5 w-3.5"
            />
        </div>
    </div>

    {/* Decorative checkbox marks */}
    <div
        class="absolute right-4 top-1/2 -translate-y-1/2 opacity-10 group-hover:opacity-20 transition-opacity"
    >
        <div class="space-y-2">
            <div class="flex items-center gap-2">
                <div
                    class="w-4 h-4 rounded border-2 border-amber-400 flex items-center justify-center"
                >
                    <Icon
                        name="check"
                        style="solid"
                        class="h-2.5 w-2.5 text-amber-400"
                    />
                </div>
                <div class="w-12 h-1 bg-amber-400 rounded"></div>
            </div>
            <div class="flex items-center gap-2">
                <div
                    class="w-4 h-4 rounded border-2 border-amber-400 flex items-center justify-center"
                >
                    <Icon
                        name="check"
                        style="solid"
                        class="h-2.5 w-2.5 text-amber-400"
                    />
                </div>
                <div class="w-10 h-1 bg-amber-400 rounded"></div>
            </div>
            <div class="flex items-center gap-2">
                <div class="w-4 h-4 rounded border-2 border-amber-400"></div>
                <div class="w-8 h-1 bg-amber-400 rounded"></div>
            </div>
        </div>
    </div>
</a>
