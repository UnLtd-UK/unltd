---
const { banner } = Astro.props;
---

<div class="flex items-center justify-between gap-x-6 bg-violet-100 px-6 py-2.5 sm:pr-3.5 lg:pl-8">
  <div class="mx-auto max-w-7xl sm:px-6 lg:px-8">
  <p class="text-sm text-violet-950">
    <a href={banner.link_url}>
      <strong class="font-semibold">{banner.body}</strong>
      {banner.link_text && banner.link_url && (
        <>
          <i class="fa-solid fa-arrow-right mx-2 inline size-0.5 fill-current"></i>
          <span class="text-violet-600 font-semibold">{banner.link_text}</span>
        </>
      )}
    </a>
  </p>
</div>
</div>