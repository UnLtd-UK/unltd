---
const { description, linkText, link } = Astro.props;
---

<div class="rounded-md bg-amber-50 p-4 mx-4 lg:mx-0">
  <div class="flex">
    <div class="flex-shrink-0">
      <i class="h-5 w-5 text-amber-400 fa-solid fa-circle-info"></i>
    </div>
    <div class="ml-3 flex-1 md:flex md:justify-between">
      <p class="text-sm text-amber-700">
        {description}
      </p>
      <p class="mt-3 text-sm md:mt-0 md:ml-6">
        <a
          href={`/${link}`}
          class="whitespace-nowrap font-medium text-amber-700 hover:text-amber-600"
        >
          {linkText}
          <span aria-hidden="true"> &rarr;</span>
        </a>
      </p>
    </div>
  </div>
</div>
