---
interface Props {
  id?: string;
  class?: string;
  bg?: string;
}

const { id, class: className, bg } = Astro.props;
---

<div id={id} class:list={[className, bg]}>
  <div class="mx-auto max-w-7xl sm:px-6 lg:px-8 px-3">
    <slot />
  </div>
</div>
