---
const { icon, textColour, bgColour, borderColour } = Astro.props;
---

<div
    class={`flex items-center justify-center h-32 w-32 rounded-2xl border shadow-2xl ${bgColour} ${borderColour}`}
>
    <i class={`${icon} text-6xl ${textColour}`}></i>
</div>
