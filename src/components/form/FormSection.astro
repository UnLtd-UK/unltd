---
const { name, description, slug } = Astro.props;
---
<form action="#" id={slug} method="POST" class="shadow sm:overflow-hidden rounded-md flex flex-col gap-10 bg-white p-10">
  <div>
    <h3 class="text-lg font-medium leading-6 text-gray-900">{ name }</h3>
    { description && <p class="mt-1 text-sm text-gray-500">{ description }</p> }
  </div>

  <div class="flex flex-col gap-8">
    <slot/>
  </div>
</form>