---
const { name, description, items, icon } = Astro.props;
---

<div class="flex gap-10 w-96">
  <div class="flex flex-col gap-4">
    <div class="flex flex-col gap-4">
      <div>
        <i class={`text-xl bg-violet-800 p-4 fa-solid fa-${icon} rounded-full`}></i>
      </div>
      <div class="flex flex-col gap-2">
        <h3 class="text-xl">{name}</h3>
        <p class="text-sm">{description}</p>
      </div>
    </div>
    <!-- <div class="flex">
      <div class="isolate flex -space-x-2 overflow-hidden">
        {
          items.map((item) => (
            <img
              class="fgf relative z-30 inline-block h-10 w-10 rounded-full ring-2 ring-white"
              src={ item.link }
              alt={ item.name }
            />
          ))
        }
      </div>
    </div> -->
  </div>
</div>
