---
import Container from "../components/Container.astro";
const { breadcrumbs } = Astro.props;
---

<Container light="violet-50" dark="violet-950">
  <div class="py-5 flex flex-col gap-12 bg-violet-50 dark:bg-violet-950">
    <div class="flex flex-row gap-5 justify-between">
      <nav class="flex" aria-label="Breadcrumb">
        <ol role="list" class="flex flex-col gap-4 md:flex-row md:items-center">
          <li>
            <div>
              <a
                href="/"
                class="hover:text-violet-700 text-violet-600 dark:hover:text-violet-300 dark:text-violet-400 transition"
              >
                <i class="fa-solid fa-house text-xs"></i>
                <span class="sr-only">UnLtd</span>
              </a>
            </div>
          </li>

          {
            breadcrumbs.map((crumb) => (
              <li>
                <div class="flex items-center">
                  <i class="fa-solid fa-chevron-right hover:text-violet-700 text-violet-600 dark:hover:text-violet-300 dark:text-violet-400 text-xs" />
                  <a
                    href={`${crumb.slug}`}
                    class="ml-4 text-xs font-medium hover:text-violet-700 text-violet-600 dark:hover:text-violet-300 dark:text-violet-400  transition"
                  >
                    {crumb.name}
                  </a>
                </div>
              </li>
            ))
          }
        </ol>
      </nav>
    </div>
  </div>
</Container>
