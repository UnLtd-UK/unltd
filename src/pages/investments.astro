---
import Template from "../layouts/Layout.astro";
const name = "Investments";
const description = "Investments description";
---

<Template name="{ `${name}` }" description="{ `${description}` }">
  <h1>{name}</h1>
  <p>{description}</p>
</Template>
