---
// PROPS
const props = {
  name: "Awards",
  slug: "/awards",
  description:
    "We offer packages of funding and support to social entrepreneurs who have an idea, or are the founder of a social venture which is making a positive difference in the UK",
};

// LAYOUT
import Template from "@layouts/Template.astro";
import Container from "@components/Container.astro";
import Breadcrumbs from "@components/Breadcrumbs.astro";

// COMPONENTS
import Header from "@components/awards/Header.astro";
import Grants from "@components/awards/Grants.astro";
import Programmes from "@components/awards/Programmes.astro";
import Steps from "@components/awards/Steps.astro";
import Questions from "@components/awards/Questions.astro";

// OTHER
import moment from "moment";
let todayDate = moment();
const breadcrumbs = [props];

// let activeGrants = rounds.filter((round) => {
//   let date = new Date();
//   let roundopen = new Date(round.open);
//   let roundclose = new Date(round.close);
//   let fundopen = new Date(round.open);
//   let fundclose = new Date(round.close);

//   if (
//     roundopen < date &&
//     roundclose > date &&
//     fundopen < date &&
//     fundclose > date
//   ) {
//     return activeGrant;
//   }
// });
---

<Template
  name={props.name}
  description={props.description}
  breadcrumbs={breadcrumbs}
>
<div class="mx-auto max-w-7xl sm:px-6 lg:px-8 flex items-center justify-between gap-x-6 bg-violet-200 px-6 py-2.5 sm:pr-3.5 lg:pl-8 rounded-xl">
  <p class="text-sm/6 text-violet-950">
    <a href="#">
      <strong class="font-semibold">GeneriCon 2023</strong><svg viewBox="0 0 2 2" class="mx-2 inline size-0.5 fill-current" aria-hidden="true"><circle cx="1" cy="1" r="1" /></svg>Join us in Denver from June 7 – 9 to see what’s coming next&nbsp;<span aria-hidden="true">&rarr;</span>
    </a>
  </p>
</div>

  <Header />
  <Grants />
  <Programmes />
  <Steps />
  <Questions />
</Template>
